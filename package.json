{
  "name": "upstream-report",
  "version": "0.0.1",
  "description": "Report changes from upstream repository ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chalk": "^1.1.3",
    "cli-table": "^0.3.1",
    "simple-git": "^1.40.0"
  },
  "bin": {
    "upstream-report": "./bin/upstream-report"
  }
}
